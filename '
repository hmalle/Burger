
<div class="container">
  <div class="jumbotron">
    <img src="/assets/img/burger.jpeg">
    <h1> Devour The Burgers </h1>
  </div>

  <div class="row">  
    <div class="col-md-5 float-left">
      <h5>Burgers ready for you</h5>
      <div id="not-eaten">
        {{#each burger}}
          {{#unless devoured}}
            {{burger_name}}
            <div class="form-group"{{this.id}}.{{this.burger_name}} >
              <button id="devour" class="btn" type="button">Devour</button>
            </div>
          {{/unless}}
        {{/each}}
      </div>
    </div>

    <div class="col-md-6">
      <h5>Burgers Eaten</h5>
      <div id="eaten" >
        {{#each burger}}
          {{#if devoured}}
            <div>
              {{burger_name}}
            </div>
          {{/if}}
        {{/each}}
      </div> 
    </div>
  </div>

  <hr>

  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <form class="create-form">
        <div class="form-group">
          <input type="text" id="burgerName" name="name">
        </div>
        <button type="submit">Add Burger</button>
      </form>
    </div>
  </div>

</div>
